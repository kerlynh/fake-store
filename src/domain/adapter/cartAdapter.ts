import { Cart } from "@/types/cartType";

export const adaptUserCartResponse = (response: Cart[]): Cart[] | undefined => {
  return response;
};

export const adaptAddCartResponse = (response: Cart): Cart | undefined => {
  return response;
};
